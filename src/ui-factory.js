import {
    Card as ACard,
    Button as AButton,
    Steps as ASteps,
    Progress as AProgress ,
    ConfigProvider as AConfigProvider  
} from 'ant-design-vue';

const components = [
    ACard,
    ASteps,
    AProgress,
    AConfigProvider,
    AButton
]

export default components;